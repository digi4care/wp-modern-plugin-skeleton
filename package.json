{
  "name": "wp-modern-plugin-skeleton",
  "version": "1.0.0",
  "description": "Modern WordPress plugin skeleton using hexagonal architecture",
  "scripts": {
    "start": "wp-env start",
    "stop": "wp-env stop",
    "clean": "wp-env clean all",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "composer install --no-dev && ./build/build.sh",
    "dev": "composer install && npm run start"
  },
  "dependencies": {
    "@wordpress/env": "^8.0.0"
  },
  "author": "Your Name",
  "license": "GPL-2.0-or-later"
}
